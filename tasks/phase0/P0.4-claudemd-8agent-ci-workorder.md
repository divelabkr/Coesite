# [Phase 0 · Task 4] P0.4 CLAUDE.md + 8-agent + scan + CI — 작업지시 #14

**참조:** `03-PROMPTS.md` P0.4 섹션, `02-AGENTS.md`, `04-SECURITY-WALL.md` §3 스캔 스크립트, `05-SCAN-PRINCIPLES.sh`.

---

## 영향 맵
- 신규: `CLAUDE.md` (루트), `docs/00~07.md` (8개), `scripts/scan-principles.sh`, `scripts/security-wall.sh`, `.github/workflows/ci.yml`, `.claude/agents/*.md` (8개)
- 수정: 없음 (단 docs/ 이동 시 `git mv` 권장 — 히스토리 보존)
- 보안·증거 인접: 인접 — **레드팀 발동**

---

## Phase
- A: 양식. B: 구현+게이트. C: 레드팀.

---

## 결정사항

### D1. 루트 CLAUDE.md (헌법 사본)
- `01-CLAUDE.md` 내용 그대로 루트 `CLAUDE.md`로 복사. 동기화 가이드 한 줄(이 파일은 01-CLAUDE.md의 사본임 명시) 헤더.

### D2. docs/ 폴더 배치
- 루트의 `00-MASTER-PLAN.md`·`01-CLAUDE.md`·`02-AGENTS.md`·`03-PROMPTS.md`·`04-SECURITY-WALL.md`·`06-VERIFICATION.md`·`07-FILE-STRUCTURE.md`를 `docs/`로 **`git mv`** (히스토리 보존). 단 `05-SCAN-PRINCIPLES.sh`는 `scripts/scan-principles.sh`로 분리(D3).
- 루트엔 `CLAUDE.md`만 남음(헌법 사본). `README.md`·`package.json` 등 빌드 파일은 그대로.

### D3. scripts/
- `scripts/scan-principles.sh`: 루트 `05-SCAN-PRINCIPLES.sh` 내용을 `git mv`로 이동, chmod +x.
- `scripts/security-wall.sh`: `docs/04-SECURITY-WALL.md` §3의 스캔 스크립트(Tier 1/2/3 키워드, regex 변형)를 추출해 신규 작성. `set -e`·`set -euo pipefail`.

### D4. .github/workflows/ci.yml
- 트리거: `on: [push, pull_request]` (`branches: [main, develop]` 한정 권장).
- jobs (4개):
  - `type-check`: `pnpm install --frozen-lockfile` → `pnpm -r exec tsc --noEmit`
  - `unit-test`: `pnpm install --frozen-lockfile` → `pnpm test`
  - `scan-principles`: `bash scripts/scan-principles.sh`
  - `security-wall`: `bash scripts/security-wall.sh` (exit 2 critical · exit 1 warning · exit 0 pass)
- 4개 모두 PASS여야 머지. `needs:` 의존성 명시.
- runs-on: `ubuntu-latest`. Node 22 (`actions/setup-node@v4 with: node-version: 22`).
- pnpm setup: `pnpm/action-setup@v4`.

### D5. .claude/agents/
- `02-AGENTS.md` §3의 8 에이전트 spec을 각 `.md` 파일로 분리:
  - `dna-guardian.md`·`gate-enforcer.md`·`schema-keeper.md`·`source-auditor.md`·`journey-guardian.md`·`security-warden.md`·`ai-engine-router.md`·`test-sentinel.md`
- 파일 형식: YAML frontmatter (name·description·model·priority) + 본문(role·triggers·checks·handoff).
- frontmatter는 Claude Code 호환 (예: name 필드는 kebab-case, model: sonnet).

### D6. .gitignore 확인 (변경 0이지만 검증)
- `node_modules/`, `dist/`, `.pnpm-store/`, `.pnpm-cache/`, `**/.env`, `**/.env.local`, `**/.env.*` 모두 유지.
- `.github/workflows/`는 ignore되면 안 됨 (CI 활성화 필수).

---

## 게이트 (8개)

1. `CLAUDE.md` 루트에 존재 + 헌법 핵심 키워드(P1~P10) 검출
2. `docs/00~07.md` 7개 파일 존재(05 제외)
3. `scripts/scan-principles.sh` + `scripts/security-wall.sh` 존재 + `bash -n` 문법 PASS
4. `scripts/scan-principles.sh` 실행 → 현재 코드 P1~P10 0 위반 PASS
5. `scripts/security-wall.sh` 실행 → Mountain 키워드 0건 PASS (exit 0)
6. `.github/workflows/ci.yml` 존재 + YAML 문법 검증(`yamllint` 또는 `python -c "import yaml; yaml.safe_load(open(...))"`)
7. `.claude/agents/` 8개 파일 존재 + 각 frontmatter 유효(YAML parse)
8. 회귀: `pnpm -r build` + `pnpm test` PASS

---

## 절대 금지
영향 맵 밖 파일 수정 / Mountain 키워드 직접 인용(스크립트 안에선 정의 필요, 단 04-SECURITY-WALL.md §1 참조 패턴 따를 것) / git commit·push.

---

## 레드팀 관점 (Phase C — 8개)
1. **CLAUDE.md 사본 drift** — 01-CLAUDE.md 원본과 루트 사본 동기화 보장 메커니즘 없음
2. **scan-principles.sh false negative** — P1 비판단 패턴 검출 누락 가능성 (예: dynamic `require('tensorflow')`)
3. **security-wall.sh false positive/negative** — 변형 키워드 정규식 우회 가능성, 또는 코드 정상 단어 충돌
4. **CI 우회** — 직접 머지·force push·CI bypass 가능한가
5. **8-agent spec drift** — `.claude/agents/`와 `02-AGENTS.md` 동기화
6. **scan 자기참조** — security-wall.sh가 자기 자신 스캔 시 무한 루프 또는 키워드 검출
7. **CI runtime 검증 부재** — Docker 없는 GH Actions에서 Postgres 게이트 NOT_RUN 패턴 그대로
8. **권한 escalation** — workflow의 permissions 명시 부재 → 기본 read-write 위험

---

## Phase A 양식

```markdown
## [P0.4] Codex 이해 확인 (Phase A)

1. 영향 맵 대조
2. D1~D6 처리 방안 한 줄씩
3. 8게이트 명령어
4. 의문점 (특히 docs/ 이동 vs 복사, 05 처리 방식)
5. 위험 3개
6. 레드팀 우선 시각
7. 약속 4개 [x]
```

---

*Work Order ID: P0.4-2026-05-22*
