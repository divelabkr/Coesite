# [P0.4 sub-2] CI 강제력 회복 + 헌법 정합 — 작업지시 #15

**범위:** BLOCKER 4 + MAJOR 5 + MINOR 2 + SUGGESTION 2 = 13건.

---

## 영향 맵
- 수정: `CLAUDE.md` (경로), `scripts/scan-principles.sh`, `scripts/security-wall.sh`, `.github/workflows/ci.yml`, `.claude/agents/gate-enforcer.md`, `.claude/agents/test-sentinel.md`, `.gitignore` (security-wall.log 제외 수정)
- 신규: `.github/CODEOWNERS`, `.github/branch-protection.md` (가이드), 그리고 drift 검사용 CI step 또는 script
- 보안·증거 인접 최강 → 레드팀 sub-2 재발동

---

## Phase
- A: 양식. B: 구현+게이트. C: 레드팀.

---

## 결정사항

### BLOCKER 4건

**B1 — SCAN_DIR 기본값**
- `scripts/scan-principles.sh`: 첫 줄 `SCAN_DIR="${SCAN_DIR:-src}"` → `SCAN_DIR="${SCAN_DIR:-packages}"` 변경. 모노레포 호환.
- `.github/workflows/ci.yml`의 scan-principles job에 `SCAN_DIR=packages` 명시(이중 안전).

**B2 — P6 SKIPPED·P8/P9/P10 warning 후 exit 0**
- `scripts/scan-principles.sh`: P6·P8·P9·P10 각 체크에서 위반 발견 시 **반드시 exit 1** 또는 누적 violations 카운터에 +1 후 종료 시 exit 1.
- P6은 단일 게이트 import 검출 로직 추가 (`@coesite/api`가 `@coesite/types` 또는 `@coesite/utils` 의존 선언 누락 시 위반).
- 진짜 검증이 어려운 항목(예: P9 cron 동작)은 "stub - manual review required"로 명시하되 violation으로 카운트 + exit 1.

**B3 — security-wall.sh 제외 범위 축소**
- 제외에서 다음만 유지: `scripts/security-wall.sh` (자기참조), `docs/04-SECURITY-WALL.md` (기준 문서)
- **제거**: `docs/00-MASTER-PLAN.md`, `tasks/phase0/*` 제외 패턴 모두 제거 → 스캔 범위에 포함.
- 단 작업지시·log 파일(`*-last.md`, `*.log`) 안의 Mountain 키워드가 정상 검토 인용일 수 있으니, 별도 allowlist 파일(`.security-wall-allowlist`) 도입하고 작업지시 등은 해당 allowlist에 등록.

**B4 — Branch protection · CODEOWNERS**
- `.github/CODEOWNERS` 신규: `* @yongjinny159` (사장님). 모든 변경에 owner approval 강제.
- `.github/branch-protection.md` 가이드 문서 신규: GitHub UI에서 main 브랜치에 다음 설정 필수 — Required status checks(4 job)·Require PR review(1+ from owners)·Require CODEOWNERS review·Disable force push·Disable deletion·Include administrators.

### MAJOR 5건

**M1 — CLAUDE.md drift 자동 검사**
- `.github/workflows/ci.yml`에 step 추가: `diff CLAUDE.md docs/01-CLAUDE.md` (헤더 sync note는 sed로 제거 후 비교). 불일치 시 fail.

**M2 — P1 grep 고급 패턴**
- `scripts/scan-principles.sh` P1 체크에 추가 패턴:
  - `require\s*\(\s*['"]tensorflow|pytorch|onnx`
  - `import\s*\(\s*['"]tensorflow|pytorch|onnx` (dynamic import)
  - `\.predict\s*\(` (이미 있음)
  - `\[\s*['"]predict['"]\s*\]` (bracket alias)

**M3 — security-wall regex 변형 확장**
- 04-SECURITY-WALL.md §8의 기존 변형 + 추가: zero-width(`​-‍`), fullwidth, 번역(사파이어·라피스 라줄리), 분할(`R[\s_-]*1[\s_-]*2`).

**M4·M5 — Agent spec 정합**
- `.claude/agents/gate-enforcer.md`의 handoff condition을 "3-gate all green" (02-AGENTS.md 원문)으로 정확히 일치시킴.
- `.claude/agents/test-sentinel.md`의 coverage 기준을 "≥ 80%"로 정확히 일치.
- 자동 검사 step: `python -c "import yaml; agents = yaml.safe_load(...); assert agents == docs_spec"` 또는 단순 grep.

### MINOR 2 + SUGGESTION 2

**N1**: `.github/workflows/ci.yml`의 checkout step에 `with: persist-credentials: false`.
**N2**: `.gitignore`의 `*.log` 제외에서 `tasks/security-wall.log` 예외 (`!tasks/security-wall.log`).
**S1**: actions SHA pinning — `actions/checkout@v4` → `actions/checkout@<sha>`. Renovate·dependabot로 관리. 우선 v4 그대로 두고 README에 향후 작업 명시.
**S2**: `security-wall` job을 `scan-principles`와 독립 — `needs: [type-check]`로만 변경 (Gemini MAJOR 1과 동일).

### codex review P2
**CLAUDE.md 부트스트랩 경로** — `CLAUDE.md` §1의 "00-MASTER-PLAN.md 읽고..."를 모두 `docs/00-MASTER-PLAN.md` 등으로 명시 수정.

---

## 게이트 (12개)

1. `bash scripts/scan-principles.sh` 실제 packages/ 스캔 + P1~P10 진짜 검증 + exit 0 PASS
2. `bash scripts/security-wall.sh` 제외 범위 축소 후 Mountain 0건 PASS
3. CLAUDE.md drift 자동 검사 step PASS
4. `.github/CODEOWNERS` 존재 + 유효 형식
5. `.github/branch-protection.md` 가이드 존재
6. ci.yml YAML 문법 PASS + `security-wall` job needs 독립 확인
7. P1 고급 패턴(dynamic require·bracket alias) detection 단위 테스트 (간이 echo + grep으로 검증)
8. security-wall regex 변형 (사파이어·R 1 2 분할 등) detection 검증
9. agent .md frontmatter + handoff·coverage 02-AGENTS.md 정합
10. CLAUDE.md 경로 모두 docs/ 형식 검증
11. .gitignore의 `!tasks/security-wall.log` 예외 검증
12. 회귀: `pnpm -r build` + `pnpm test` PASS

---

## 절대 금지
영향 맵 밖 파일 수정 / Mountain 키워드 노출 (단 04-SECURITY-WALL.md·security-wall.sh의 정의 부분은 예외) / git commit·push / 가짜 PASS 포장.

---

## Phase A 양식
1. 영향 맵 대조
2. BLOCKER 4·MAJOR 5·MINOR 2·SUGGESTION 2 + codex review P2 처리 방안 한 줄씩 (13건)
3. 12게이트 명령어
4. 의문점 (특히 B3 allowlist 파일 형식·M3 regex 정확도)
5. 위험 3개
6. 약속 4개 [x]

---

*Work Order ID: P0.4-sub-2-2026-05-22*
