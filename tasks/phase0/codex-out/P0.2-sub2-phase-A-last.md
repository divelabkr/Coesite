## [P0.2 sub-cycle 2] Codex 이해 확인 (Phase A)

1. 영향 맵 대조 (✅·❌)
- ✅ 수정 대상: `.gitignore`, `infra/docker-compose.yml`, `infra/.env.example`, `packages/api/.env.example`, `README.md`
- ✅ 신규 파일: 없음
- ✅ 의존성 추가: 없음
- ✅ 보안·증거 인접 변경: 유지, Phase C 레드팀 재발동 대상

2. BLOCKER 2 처리 방안 복창 (B1·B2)
- B1: Docker가 있으면 게이트 1~5 실제 실행. 없으면 `NOT_RUN`으로 정직 보고. Docker 도착 시 별도 호출로 게이트 2~5 재검증.
- B2: `.gitignore`에 `**/.env`, `**/.env.local`, `.pnpm-store/` 추가. 기존 루트 전용 `.env`, `.env.local`은 포괄 규칙으로 대체 가능.

3. MAJOR 10 처리 방안 복창
- M-codex review 1: `.pnpm-store/` ignore 추가, 이미 추적 중인 캐시 없음 확인.
- M-codex review 2: `env_file`을 객체가 아닌 `- ./.env` 문자열 형식으로 변경.
- M-Gemini 1: README에 사전 요구사항, 프로젝트 시작, 세션 시작 의무, `pnpm test` 검증 방법 추가.
- M-Gemini 2: 향후 `api` 서비스 추가 시 `depends_on` health condition 주석 추가.
- M-레드팀 M1·M2: Redis 비밀번호 적용, Postgres/Redis 포트를 `127.0.0.1`에만 bind.
- M-레드팀 M3: README에 `down`/`down -v` 차이와 시크릿 변경 후 `down -v` 권고 추가.
- M-레드팀 M4: Postgres/Redis 이미지를 digest로 pin. Docker 부재 시 `NOT_RUN` 또는 Docker Hub API 조회 여부 기록.
- M-레드팀 M5: `JWT_SECRET` placeholder 강화, README에 `openssl rand -hex 32` 절차 추가.
- M-레드팀 M6: Postgres/Redis healthcheck를 인증 검증 가능한 명령으로 강화.

4. MINOR 7 / SUGGESTION 6 처리 방안 요약
- MINOR: `DATABASE_URL` 내부/외부 주석, `scram-sha-256` 확인, `0.0.0.0` 금지 경고, `security_opt`/`cap_drop` 추가, init args는 첫 초기화에만 적용됨 명시, 프로덕션 secrets manager 권고, 프로덕션 container/volume 이름 무작위화 권고.
- SUGGESTION: DevContainer, WORM 관리자 계정, debug profile은 별도 task. retry/backoff, 시크릿 생성/교체, volume purge·백업은 MAJOR 처리에 흡수.

5. 9게이트 명령어 복창
- `docker compose --env-file ./infra/.env.example -f ./infra/docker-compose.yml config`
- 임시 `.env` 생성 후 `docker compose up -d`, 30초 대기, `docker compose ps` healthy 확인
- `docker exec coesite-postgres psql -U coesite -d coesite_dev -c "SELECT 1"`
- `docker exec coesite-redis redis-cli -a "$REDIS_PASSWORD" ping`
- `docker compose down -v`
- Mountain 키워드 `rg --fixed-strings` 0건 확인
- 시크릿 패턴 `AWS_`, `AKIA`, `-----BEGIN`, real-looking 0건 확인
- `git check-ignore infra/.env packages/api/.env .pnpm-store/test`
- `docker-compose.yml`에서 `127.0.0.1` bind, `security_opt`, `cap_drop` 존재 정적 점검

6. Docker 부재 시 처리 (NOT_RUN 정직 보고)
- Docker 의존 게이트는 실행하지 않은 것으로 `NOT_RUN` 명시.
- Docker 부재를 성공처럼 포장하지 않음.
- Docker 도착 후 별도 호출에서 게이트 2~5만 재검증 가능하다고 기록.

7. 의문점·확인
- 없음

8. 예상 위험 3개
- digest pin 조회가 Docker/네트워크 상태에 의존해 Phase B에서 보류될 수 있음.
- Redis password를 command/env로 넘기는 방식은 개발 환경 한계상 완전한 secret 은닉이 아님.
- `security_opt`/`cap_drop` 적용 후 이미지 기본 동작이나 healthcheck가 환경에 따라 실패할 수 있음.

9. Phase C 레드팀 우선 시각
- fortify 적용 후에도 남는 위험을 우선 발굴.
- 특히 digest pin 신뢰성, Redis 인증 노출 한계, volume 잔존 데이터, host port 노출, healthcheck 우회 가능성, README 절차 누락을 중점 확인.

10. 약속 4개 [x]
- [x] Phase A에서는 구현하지 않는다.
- [x] 파일을 수정하지 않는다.
- [x] 불확실하거나 미실행한 항목은 `NOT_RUN` 또는 미확인으로 정직하게 쓴다.
- [x] Phase B/C는 별도 호출 없이는 진행하지 않는다.