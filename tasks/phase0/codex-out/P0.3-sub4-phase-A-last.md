1. 영향 맵 대조  
- 수정 대상: `prisma/migrations/20260522000002_chain_integrity/migration.sql`, `pnpm-lock.yaml`
- 금지: 영향 맵 밖 파일 수정, 운영 layer 시도, git commit/push

2. M1·M2·M3 처리 방안 한 줄씩  
- M1: `verify_chain_insert()`의 모든 동적 SQL을 `TG_TABLE_SCHEMA + TG_TABLE_NAME` 기반 schema-qualified 참조로 변경한다.
- M2: `pnpm install`로 `dotenv-cli` 의존성을 lockfile에 반영하고 frozen lockfile로 검증한다.
- M3: 4개 WORM 테이블의 `prevHash` global unique를 제거하고 각 자연키 scope 복합 unique 및 scope-aware 트리거 검증으로 바꾼다.

3. 6게이트 명령어  
- `pnpm prisma validate`
- `pnpm prisma format`
- `pnpm install --frozen-lockfile`
- migration 정적 grep: `TG_TABLE_SCHEMA`, 4개 scope unique 제약, global `prevHash` unique 잔존 0건 확인
- `pnpm prisma migrate dev`
- `pnpm -r build && pnpm test` + Mountain 0건 확인

4. 의문점  
없음

5. 위험 3개  
- 기존 global `prevHash` unique 제거가 Prisma schema와 migration SQL 사이에서 불일치할 위험
- scope-aware GENESIS 검증 누락 시 동일 scope 내 체인 무결성이 깨질 위험
- Docker/DB 부재 시 `prisma migrate dev`가 NOT_RUN으로 남아 실제 DB 적용 검증이 지연될 위험

6. 약속 4개 [x]  
- [x] 영향 맵 밖 파일은 수정하지 않는다.
- [x] 운영 layer 작업은 하지 않는다.
- [x] git commit·push는 하지 않는다.
- [x] 구현 후 결과는 `docs/codex-log/`에 실패·미검증 포함해 그대로 기록한다.