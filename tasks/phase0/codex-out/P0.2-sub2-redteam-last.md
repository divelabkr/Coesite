수정 없음. 요청대로 읽기 전용 레드팀 검토만 수행했다. `docs/codex-log` 요약본도 새로 만들지 않았다.

**BLOCKER**
- B1. 런타임 게이트 5개가 `NOT_RUN` 상태라 fortify가 실제 Docker 실행에서 유효한지 증명되지 않았다. `docker compose config`, `up/healthy`, Postgres `SELECT 1`, Redis authenticated ping, `down -v`가 미검증이다. Compose 보안 변경은 정적 존재만 확인된 상태라 P0.2 완료 신뢰성이 부족하다. 근거: [docs/codex-log/2026-05-22-P0.2-sub-cycle-2.md](C:/My_Project/Coesite/docs/codex-log/2026-05-22-P0.2-sub-cycle-2.md:50)

**MAJOR**
- M1. Redis password가 command/healthcheck 인자에 노출된다. `--requirepass ${REDIS_PASSWORD}`와 `redis-cli -a`는 `docker inspect`, 프로세스 인자, 로그/진단 출력에 잡힐 수 있다. 근거: [infra/docker-compose.yml](C:/My_Project/Coesite/infra/docker-compose.yml:35), [infra/docker-compose.yml](C:/My_Project/Coesite/infra/docker-compose.yml:47)
- M2. placeholder credential 사용을 구조적으로 막지 않는다. `change-me-locally`가 Postgres/Redis/API URL에 그대로 있고 README는 Redis만 명시적으로 교체하라고 한다. 로컬 포트가 열려 있으면 known credential 공격면이 남는다. 근거: [infra/.env.example](C:/My_Project/Coesite/infra/.env.example:4), [packages/api/.env.example](C:/My_Project/Coesite/packages/api/.env.example:3)
- M3. `127.0.0.1` bind는 외부 노출은 줄였지만 로컬 악성 프로세스, 다른 로컬 사용자, SSRF형 로컬 호출에는 DB/Redis가 API 권한 계층을 우회해 노출된다. 근거: [infra/docker-compose.yml](C:/My_Project/Coesite/infra/docker-compose.yml:17), [infra/docker-compose.yml](C:/My_Project/Coesite/infra/docker-compose.yml:39)
- M4. named volume이 고정명으로 남아 stale PII/secret/auth 설정이 재사용될 수 있다. README는 `down -v`를 권장하지만 시작 절차에 기존 volume 탐지/차단이 없다. 근거: [infra/docker-compose.yml](C:/My_Project/Coesite/infra/docker-compose.yml:59), [README.md](C:/My_Project/Coesite/README.md:54)
- M5. digest pin은 적용됐지만 갱신·취약점 스캔·출처 검증 절차가 없다. 2026-05-22 조회 기준 Docker Hub의 현재 `postgres:16-alpine`, `redis:7-alpine` 태그 digest는 산출물 digest와 달라, 패치가 자동 반영되지 않는 상태다. 단독 악성 증거는 아니지만 공급망 운영 리스크다. 출처: [Postgres tags](https://hub.docker.com/_/postgres/tags?name=16&page=1), [Redis tags](https://hub.docker.com/_/redis/tags?name=7&page=1)
- M6. healthcheck가 “서비스가 켜짐 + privileged credential로 ping/query 가능”만 본다. 앱용 최소권한 계정, read/write, persistence, migration readiness는 검증하지 않고, 향후 API `depends_on`도 주석뿐이라 race가 재발할 수 있다. 근거: [infra/docker-compose.yml](C:/My_Project/Coesite/infra/docker-compose.yml:25)
- M7. README에 `packages/api/.env` 생성 및 `infra/.env`와의 credential 동기화 절차가 없다. 사용자는 infra secret을 바꿔도 API URL placeholder를 그대로 두거나 불일치시킬 수 있다. 근거: [README.md](C:/My_Project/Coesite/README.md:23), [packages/api/.env.example](C:/My_Project/Coesite/packages/api/.env.example:3)

**MINOR**
- N1. `.gitignore`가 `**/.env`, `**/.env.local`만 막고 `.env.production`, `.env.development`, `.env.backup`, `*.env`류는 막지 않는다. 근거: [.gitignore](C:/My_Project/Coesite/.gitignore:4)
- N2. Postgres password가 `POSTGRES_PASSWORD`와 `PGPASSWORD`로 중복 env 노출된다. 근거: [infra/docker-compose.yml](C:/My_Project/Coesite/infra/docker-compose.yml:10)
- N3. `restart: unless-stopped`로 재부팅 후 stale volume과 host port가 다시 살아날 수 있다. 근거: [infra/docker-compose.yml](C:/My_Project/Coesite/infra/docker-compose.yml:5)
- N4. 고정 `container_name`/volume name은 enumeration과 프로젝트 간 충돌을 쉽게 한다. 근거: [infra/docker-compose.yml](C:/My_Project/Coesite/infra/docker-compose.yml:4)
- N5. `cap_drop`/`no-new-privileges`는 있으나 명시적 `user`, `read_only`, tmpfs/resource limit은 없다. 개발 DB 특성상 일부는 trade-off지만 DoS/탈출 blast radius가 남는다.
- N6. 종료 절차가 `infra/.env`만 삭제한다. `packages/api/.env`, shell history, Docker metadata, volume 잔존 secret은 정리 범위 밖이다. 근거: [README.md](C:/My_Project/Coesite/README.md:49)

**SUGGESTION**
- S1. 시작 전 preflight로 placeholder/empty password, `0.0.0.0` bind, 기존 volume 존재를 차단한다.
- S2. 기본 compose에서는 host port를 닫고, 필요 시 debug profile에서만 5432/6379를 연다.
- S3. Redis는 ACL user와 제한 권한을 쓰고, secret은 command 인자 대신 secrets 또는 `*_FILE` 패턴으로 옮긴다.
- S4. digest 갱신일, 조회 출처, CVE scan 결과, 의도적 freeze 사유를 README나 로그에 남긴다.
- S5. volume은 환경/브랜치별 namespace 또는 무작위 project name을 쓰고 백업 제외/폐기 절차를 명령으로 고정한다.

[P0.2 sub-2 레드팀 완료] BLOCKER 1 / MAJOR 7 / MINOR 6 / SUGGESTION 5