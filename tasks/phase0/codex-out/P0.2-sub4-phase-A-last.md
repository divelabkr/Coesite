## [P0.2 sub-4] Codex 이해 확인 (Phase A)

1. 영향 맵 대조  
README.md, infra/preflight.sh, package.json 3파일만 해당.

2. D1 README 단계 추가 복창  
Quick start는 `cp infra/.env.example infra/.env` 후 placeholder 실제 값 교체, `cp packages/api/.env.example packages/api/.env` 후 URL password 동기화 및 JWT_SECRET 교체, 마지막에 `pnpm db:up` 실행 순서로 명시한다.

3. D2 preflight JWT 패턴 복창  
`infra/preflight.sh`에서 다음을 차단/경고한다:  
`JWT_SECRET=__REPLACE_WITH_RANDOM_32+_BYTES_FROM_KMS__` 거부, `JWT_SECRET=change-me*` 거부, `JWT_SECRET=` 빈값 거부, 32자 미만은 경고.

4. D3 package.json scripts 복창  
루트 `package.json` scripts에 `db:up`, `db:down`, `db:reset` 추가.  
README에서는 직접 `docker compose up` 안내를 제거하고 `pnpm db:up`만 안내한다.

5. 5게이트 명령어  
`bash -n infra/preflight.sh`  
README 정적 검증: `.env.example` 복사 → 값 교체 → `pnpm db:up` 순서 grep 확인  
package.json 정적 검증: `db:up`, `db:down`, `db:reset` 존재 확인  
preflight 정적 검증: JWT_SECRET placeholder 3종 reject 패턴 검출  
`pnpm -r build` + `pnpm test`

6. 의문점  
없음

7. 위험 2개  
README의 기존 `docker compose up` 직접 호출 라인이 남으면 preflight 강제력이 우회될 수 있음.  
JWT_SECRET 패턴 검사가 너무 좁으면 변형된 placeholder나 짧은 비밀값을 놓칠 수 있음.

8. 약속 4개 [x]  
[x] 영향 맵 밖 파일 수정 금지  
[x] git commit·push 금지  
[x] 헌법 문서 수정 금지  
[x] Mountain 키워드 노출 금지