**BLOCKER**
- `verify_chain_insert()`가 동적 SQL에서 `%I` + `TG_TABLE_NAME`만 써서 schema를 고정하지 않습니다. 런타임 role이 `TEMP`/`search_path`로 같은 이름의 temp table을 만들 수 있으면 `prevHash` 존재 검증을 속여 끊어진 체인을 삽입할 수 있습니다: [chain migration](C:/My_Project/Coesite/prisma/migrations/20260522000002_chain_integrity/migration.sql:34), [role migration](C:/My_Project/Coesite/prisma/migrations/20260522000003_role_separation/migration.sql:7).

**MAJOR**
- Runtime role은 실제 앱 경로에 연결되지 않았습니다. Prisma datasource는 여전히 `DATABASE_URL`만 보고, `RUNTIME_DATABASE_URL`은 예시에만 있습니다: [schema.prisma](C:/My_Project/Coesite/prisma/schema.prisma:7), [infra/.env.example](C:/My_Project/Coesite/infra/.env.example:8).
- `PASSWORD NULL`은 현재 Docker의 `scram-sha-256` host/local 설정에서는 password login을 막지만, role 자체는 `LOGIN`입니다. 다른 pg_hba가 `trust/peer/cert`이면 접속 가능하고, 기존 role 속성도 검증하지 않습니다: [role migration](C:/My_Project/Coesite/prisma/migrations/20260522000003_role_separation/migration.sql:2).
- `IF NOT EXISTS`는 중복 실행은 흡수하지만 동시 migration race에는 안전하지 않습니다. 두 runner가 동시에 role 부재를 보면 한쪽 `CREATE ROLE`은 실패할 수 있고, 이미 존재하는 `coesite_runtime`이 안전한 속성인지 확인하지 않습니다: [role migration](C:/My_Project/Coesite/prisma/migrations/20260522000003_role_separation/migration.sql:1).
- `dotenv-cli`가 `package.json`에는 추가됐지만 `pnpm-lock.yaml` importer에는 없습니다. frozen install/CI 재현성이 깨지고 실제 설치 버전 고정도 안 됩니다: [package.json](C:/My_Project/Coesite/package.json:21), [pnpm-lock.yaml](C:/My_Project/Coesite/pnpm-lock.yaml:7).

**MINOR**
- 순수 제약 기준으로 fork 차단은 개선됐습니다. `AuditLog`는 `agentId + prevHash`, 나머지 4개 WORM 테이블은 전역 `prevHash UNIQUE`라 같은 head에서 두 child를 만드는 기본 fork는 막힙니다: [chain migration](C:/My_Project/Coesite/prisma/migrations/20260522000002_chain_integrity/migration.sql:4).
- `hash='GENESIS'` 제외 후 빈 테이블 첫 삽입은 `prevHash='GENESIS'` + 64hex `hash`이면 통과합니다. 다만 자동 genesis seed path는 보이지 않습니다: [chain migration](C:/My_Project/Coesite/prisma/migrations/20260522000002_chain_integrity/migration.sql:5).
- `dotenv-cli` 자체는 `.env` 로딩을 명시화한 수준이지만, Prisma migrate가 `packages/api/.env`를 무조건 신뢰하므로 잘못된 `DATABASE_URL`로 dev migration을 실행할 위험은 남습니다: [package.json](C:/My_Project/Coesite/package.json:14).

**SUGGESTION**
- 트리거 SQL은 schema-qualified table로 고정하고, runtime role의 `TEMP`/`search_path`를 차단하거나 함수 내부에서 안전한 `search_path`를 고정해야 합니다.
- role migration은 기존 role 속성 검증, advisory lock 또는 migration 단일 실행 보장, runtime/app datasource 분리를 같이 묶어야 합니다.

검증: 정적 검토만 수행, DB/Docker/동시성 실험은 실행하지 않음. 종료: P0 통과 불가.