BLOCKER: 없음; MAJOR: `infra/preflight.sh:108` API `.env` 누락 시 JWT 검증이 경고로 통과하고 `:65-76`은 placeholder 변형/inline comment/공백을 우회 가능, `README.md:38-45`는 기본 `db:up` 후 127.0.0.1 API 접속을 유도하지만 debug port 절차가 없어 실패 유발; MINOR: `package.json:12` Windows/Powershell 흐름에서 `rm -f` 실패 가능; SUGGESTION: 직접 `docker compose` 우회는 막을 수 없으니 README에서 `pnpm db:up`만 승인 경로로 못박아야 함.