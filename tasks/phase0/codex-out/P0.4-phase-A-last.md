## [P0.4] Codex 이해 확인 (Phase A)

1. 영향 맵 대조  
- 신규: `CLAUDE.md`, `docs/00~07.md`, `scripts/scan-principles.sh`, `scripts/security-wall.sh`, `.github/workflows/ci.yml`, `.claude/agents/*.md`
- 수정: 없음
- 이동: 기존 루트 문서 7개는 `docs/`로, `05-SCAN-PRINCIPLES.sh`는 `scripts/scan-principles.sh`로 `git mv`
- 보안·증거 인접 작업이므로 레드팀 관점 필수

2. D1~D6 처리 방안 한 줄씩  
- D1: `01-CLAUDE.md` 내용을 루트 `CLAUDE.md`로 복사하고 사본임을 명시한다.
- D2: 지정된 7개 문서를 `docs/`로 `git mv`한다.
- D3: `05-SCAN-PRINCIPLES.sh`를 `scripts/scan-principles.sh`로 이동하고 `security-wall.sh`를 신규 작성한다.
- D4: GitHub Actions CI를 4개 job으로 구성하고 Node 22, pnpm 기반으로 실행한다.
- D5: `02-AGENTS.md` §3의 8개 에이전트를 `.claude/agents/*.md`로 분리한다.
- D6: `.gitignore`가 필수 ignore 항목을 유지하고 `.github/workflows/`를 막지 않는지 확인한다.

3. 8게이트 명령어  
- `test -f CLAUDE.md && grep -E "P1|P2|P3|P4|P5|P6|P7|P8|P9|P10" CLAUDE.md`
- `test -f docs/00-MASTER-PLAN.md && test -f docs/01-CLAUDE.md && test -f docs/02-AGENTS.md && test -f docs/03-PROMPTS.md && test -f docs/04-SECURITY-WALL.md && test -f docs/06-VERIFICATION.md && test -f docs/07-FILE-STRUCTURE.md`
- `bash -n scripts/scan-principles.sh && bash -n scripts/security-wall.sh`
- `bash scripts/scan-principles.sh`
- `bash scripts/security-wall.sh`
- `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml', encoding='utf-8'))"`
- `python -c "import pathlib, yaml; [yaml.safe_load(p.read_text(encoding='utf-8').split('---')[1]) for p in pathlib.Path('.claude/agents').glob('*.md')]"`
- `pnpm -r build && pnpm test`

4. 의문점  
- `docs/` 이동은 복사가 아니라 `git mv`가 맞는지 최종 확인 필요.
- `05-SCAN-PRINCIPLES.sh`는 `docs/05...`로 두지 않고 `scripts/scan-principles.sh`로만 이동하는 것이 맞는지 확인 필요.
- 루트 `01-CLAUDE.md`를 이동한 뒤 루트 `CLAUDE.md` 사본 생성 순서가 drift 검증에 영향을 주는지 확인 필요.

5. 위험 3개  
- `CLAUDE.md`와 `docs/01-CLAUDE.md` 사본 drift 위험
- 보안 스캔 regex의 false positive / false negative 위험
- CI 권한·브랜치 제한·bypass 설정 미흡 위험

6. 레드팀 우선 시각  
- 1순위: `security-wall.sh` 자기참조 및 금지 키워드 검출 우회 가능성
- 2순위: CI 우회 가능성과 workflow permissions 기본값 위험
- 3순위: 문서 원본과 생성 산출물 간 drift 위험

7. 약속 4개 [x]  
- [x] 영향 맵 밖 파일은 수정하지 않는다.
- [x] 구현 전 게이트 기준을 먼저 고정한다.
- [x] 실패·미검증·건너뜀은 요약에 그대로 남긴다.
- [x] git commit·push는 하지 않는다.