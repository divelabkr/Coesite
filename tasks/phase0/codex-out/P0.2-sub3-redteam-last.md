수정 없음. 요청대로 읽기 전용 레드팀 검토만 수행했다. Docker와 `bash` 실행은 현재 환경에서 재검증하지 못했으므로 런타임 동작은 미확정이다.

**BLOCKER**
- B1. `preflight.sh`는 강제력이 없다. README에서 실행을 안내할 뿐, `docker compose --env-file ./infra/.env -f ./infra/docker-compose.yml up -d`를 직접 실행하면 placeholder·stale volume·0.0.0.0 검사를 모두 우회할 수 있다. `package.json`에도 preflight 래퍼가 없다. 근거: [README.md](C:/My_Project/Coesite/README.md:38), [README.md](C:/My_Project/Coesite/README.md:39), [package.json](C:/My_Project/Coesite/package.json:6)
- B2. Docker 런타임 게이트 1~5가 여전히 `NOT_RUN`이다. `cap_drop`, non-root `user`, debug proxy, healthcheck가 실제로 부팅·차단·노출 면에서 유효한지 증명되지 않았다. 근거: [docs/codex-log/2026-05-22-P0.2-sub-cycle-3.md](C:/My_Project/Coesite/docs/codex-log/2026-05-22-P0.2-sub-cycle-3.md:47)

**MAJOR**
- M1. narrow `cap_drop`은 “진짜 위험만 제거”라는 근거가 부족하다. `NET_RAW`, `SYS_ADMIN` 등은 제거됐지만 Docker 기본 capability 중 무엇을 실제로 보존해야 하는지 실행 증거가 없다. 특히 `cap_drop: ALL + cap_add 최소셋`과 달리 기본 허용 capability가 넓게 남는 구조다. 근거: [infra/docker-compose.yml](C:/My_Project/Coesite/infra/docker-compose.yml:25), [infra/docker-compose.yml](C:/My_Project/Coesite/infra/docker-compose.yml:62)
- M2. `preflight.sh`는 실행해도 rendered compose를 검증하지 않는다. 파일에서 `change-me-locally`와 `0.0.0.0` 문자열만 찾기 때문에 shell env override, compose 파일의 `ports` 변경, 변수 치환 결과의 위험값을 잡지 못한다. 근거: [infra/preflight.sh](C:/My_Project/Coesite/infra/preflight.sh:37), [infra/preflight.sh](C:/My_Project/Coesite/infra/preflight.sh:45), [infra/preflight.sh](C:/My_Project/Coesite/infra/preflight.sh:85)
- M3. debug profile은 새 공격면을 만든다. `alpine/socat:1.8.0.3`는 digest pin이 아니고, 별도 프록시 컨테이너가 DB 내부 네트워크 접근 권한을 갖고 로컬 포트를 연다. loopback이라 외부 노출은 줄지만 로컬 악성 프로세스·SSRF·다중 사용자 로컬 환경에는 API 계층 우회 경로다. 근거: [infra/docker-compose.yml](C:/My_Project/Coesite/infra/docker-compose.yml:92), [infra/docker-compose.yml](C:/My_Project/Coesite/infra/docker-compose.yml:98), [infra/docker-compose.yml](C:/My_Project/Coesite/infra/docker-compose.yml:99)
- M4. Redis password 노출은 완전히 해소되지 않았다. healthcheck의 `redis-cli -a`는 제거됐지만 `redis-server --requirepass ${REDIS_PASSWORD}`는 여전히 command 인자에 남아 inspect/process 진단 표면에 노출될 수 있다. 근거: [infra/docker-compose.yml](C:/My_Project/Coesite/infra/docker-compose.yml:52)
- M5. 정책 문서 변경은 sub-3 영향 맵을 벗어났다. 작업지시의 수정 대상은 `.gitignore`, `infra/*`, `packages/api/.env.example`, `README.md`였는데 헌법성 문서와 P0.1 작업지시까지 변경했다. 범위 일탈이다. 근거: [tasks/phase0/P0.2-sub3-workorder-full.md](C:/My_Project/Coesite/tasks/phase0/P0.2-sub3-workorder-full.md:8), [docs/codex-log/2026-05-22-P0.2-sub-cycle-3.md](C:/My_Project/Coesite/docs/codex-log/2026-05-22-P0.2-sub-cycle-3.md:40)
- M6. 금지어 직접 열거를 참조형으로 바꾸면서 독립 실행성은 약해졌다. `04-SECURITY-WALL.md`를 반드시 읽는 세션에서는 유지되지만, 작업지시·요약본만 복사되는 흐름에서는 실제 금지 패턴이 사라져 스캔 기준이 느슨해질 수 있다. 근거: [01-CLAUDE.md](C:/My_Project/Coesite/01-CLAUDE.md:143), [02-AGENTS.md](C:/My_Project/Coesite/02-AGENTS.md:226), [tasks/phase0/P0.1-monorepo-workorder.md](C:/My_Project/Coesite/tasks/phase0/P0.1-monorepo-workorder.md:66)

**MINOR**
- N1. preflight password 검사는 “빈 문자열”만 본다. 공백값, `export KEY=`, 앞뒤 공백, unresolved placeholder류는 정밀 검증하지 않는다. 근거: [infra/preflight.sh](C:/My_Project/Coesite/infra/preflight.sh:24), [infra/preflight.sh](C:/My_Project/Coesite/infra/preflight.sh:50)
- N2. `packages/api/.env`가 없으면 경고만 하고 통과한다. README는 credential 동기화를 요구하지만 preflight는 API env 존재와 DB/Redis URL 일치성을 강제하지 않는다. 근거: [infra/preflight.sh](C:/My_Project/Coesite/infra/preflight.sh:90), [README.md](C:/My_Project/Coesite/README.md:56)
- N3. debug proxy 서비스에는 `user`, `read_only`, resource limit이 없다. `cap_drop: ALL`은 있지만 새 컨테이너 표면에 동일한 hardening 기준이 적용되지 않았다. 근거: [infra/docker-compose.yml](C:/My_Project/Coesite/infra/docker-compose.yml:101), [infra/docker-compose.yml](C:/My_Project/Coesite/infra/docker-compose.yml:103)
- N4. 고정 `container_name`과 고정 volume name은 여전히 enumeration과 프로젝트 충돌 표면이다. 운영 backlog로 남긴 판단은 이해되지만 P0.2 로컬 공격면에서는 잔존한다. 근거: [infra/docker-compose.yml](C:/My_Project/Coesite/infra/docker-compose.yml:8), [infra/docker-compose.yml](C:/My_Project/Coesite/infra/docker-compose.yml:125)

**SUGGESTION**
- S1. compose 실행은 `pnpm db:up` 같은 단일 래퍼로 고정하고 그 안에서 `preflight && docker compose config && up`을 실행하게 한다.
- S2. debug profile은 별도 override 파일로 분리하고, `socat`도 digest pin·non-root·read_only 기준을 맞춘다.
- S3. capability는 “보존 이유 표 + 런타임 `capsh --print` 증거”를 게이트로 추가한다.
- S4. `04-SECURITY-WALL.md`의 regex variant 목록을 실제 스크립트/CI에 반영해 참조형 문서 변경의 약화를 보완한다.

[P0.2 sub-3 레드팀 완료] BLOCKER 2 / MAJOR 6 / MINOR 4 / SUGGESTION 4