보고서 파일 `docs/codex-log/2026-05-22-P0.2-docker-compose-redteam.md` 작성은 시도했지만, 현재 세션이 `read-only sandbox`라 차단되었습니다. 코드/산출물 수정은 하지 않았습니다.

**BLOCKER**
- B1. `README.md:12`가 `infra/.env` 생성을 지시하지만 `.gitignore`는 루트 `.env`만 보호함. 실제 DB/JWT 시크릿이 `git add infra/`로 유출될 수 있음.

**MAJOR**
- M1. Redis 인증 없음 + host port 공개.
- M2. Postgres/Redis host port 공개로 API 권한 계층 우회 가능.
- M3. named volume이 `down` 후에도 남아 PII/시크릿 잔존 및 백업 유출 가능.
- M4. `postgres:16-alpine`, `redis:7-alpine` digest pin 부재.
- M5. JWT/DB placeholder를 그대로 쓰면 replay/token forgery 가능.
- M6. healthcheck가 인증/쿼리 가능성을 검증하지 않아 false healthy 및 API race 가능.

**MINOR**
- N1. 기본 bind는 `127.0.0.1`이나 `0.0.0.0` override 방지/경고 없음.
- N2. compose 레벨 `user`, `read_only`, `cap_drop`, `security_opt` 없음.
- N3. Postgres `POSTGRES_INITDB_ARGS`는 최초 init에만 적용되어 기존 volume의 약한 인증 설정 잔존 가능.
- N4. `POSTGRES_PASSWORD`, `PGPASSWORD`가 컨테이너 env로 노출됨.
- N5. 고정 container/volume/user/db/port 값이 enumeration을 쉽게 함.

**SUGGESTION**
- S1. P0.3 API 추가 시 `depends_on.condition: service_healthy`와 앱 retry/backoff 필요.
- S2. README에 시크릿 생성/교체/검증 절차 필요.
- S3. DB/Redis host port 공개를 debug profile로 분리 검토.
- S4. volume purge와 백업 제외 정책을 README에 명시 필요.

실제 AWS/AKIA/private key 형태의 시크릿과 내부 도메인/IP/인증 흐름 직접 노출은 발견하지 못했습니다.

[P0.2 레드팀 완료] BLOCKER 1 / MAJOR 6 / MINOR 5 / SUGGESTION 4