1. 영향 맵 대조

- 신규: `prisma/migrations/<TS>_drop_stale_audit_index/migration.sql`, `prisma/migrations/<TS>_hash_recompute_intent/migration.sql`
- 수정: `prisma/schema.prisma`, `infra/.env.example`, `packages/api/.env.example`, `package.json`, `README`, `docs/codex-log/2026-05-22-mythos-doom-followup.md`
- 범위 밖 파일 수정 금지.

2. D1~D5 처리 방안

- D1: owner/runtime DB URL을 env와 script 수준에서 분리하되, Prisma client 다중 datasource 실제 적용은 Phase 1 backlog로 남김.
- D2: hash 형식과 `hash != prevHash` 검증을 migration으로 강화하되, canonical payload digest 재계산은 Phase 1 application layer로 남김.
- D3: WORM payload/message PII 경고 주석을 강화하되, envelope encryption 구현은 Phase 1로 남김.
- D4: `DROP INDEX IF EXISTS "AuditLog_hash_idx";` migration 추가로 schema drift는 P0.x에서 완료 처리.
- D5: Docker 설치/실행 검증은 별도 트랙으로 분리하고, README에는 수동 설치 및 대안만 기록.

3. 6게이트 명령어

```powershell
pnpm prisma validate --schema prisma/schema.prisma
pnpm prisma format --schema prisma/schema.prisma
Select-String -Path "prisma/migrations/*/migration.sql" -Pattern "drop_stale_audit_index","hash_recompute_intent","TODO[Phase 1+]"
Select-String -Path "infra/.env.example","packages/api/.env.example" -Pattern "DATABASE_URL_OWNER","RUNTIME_DATABASE_URL"
pnpm -r build; pnpm test
rg -n "<금지어>" .
```

4. 의문점·위험

- 의문점: `<TS>` migration timestamp는 기존 migration 네이밍 규칙을 확인한 뒤 맞춰야 함.
- 위험: P0.x는 env/schema/migration/문서 중심이라 runtime role 실제 사용과 hash 재계산 보장은 Phase 1 전까지 미완성 상태로 남음.

5. 약속 4개

- [x] 영향 맵 밖 파일은 수정하지 않음.
- [x] application layer 코드는 추가하지 않음.
- [x] git commit·push는 하지 않음.
- [x] 구현 후 검증 결과와 미검증 항목을 codex-log에 그대로 기록함.