# [P0.1] 보강 작업지시 #2 — Phase B 재실행

**발행자:** Claude
**수신자:** Codex
**일자:** 2026-05-21 (재실행)
**참조 필수:**
- `tasks/phase0/P0.1-monorepo-workorder.md` (원지시)
- `tasks/phase0/P0.1-monorepo-workorder-supplement.md` (1차 보강)
- 이 파일 (2차 보강 — 1차 실패 원인 복구)

---

## 0. 1차 Phase B 실패 요약

이전 시도(Phase B-1)는 `pnpm`이 호스트에 부재 + Codex 샌드박스 네트워크 차단으로 `npm install -g pnpm` 실패 → 게이트 0/5 보고 후 정상 중단. P2 Fail-Closed 정신대로 멈춘 것은 옳은 동작.

---

## 1. 변경된 환경 (Claude가 사전 처리 완료)

- ✅ **pnpm 10.33.4 호스트 전역 설치 완료** (`/c/Users/yongj/AppData/Roaming/npm/pnpm`).
- ✅ **이번 Codex 호출은 `-c sandbox_workspace_write.network_access=true`** 로 네트워크 허용된 상태로 호출됨.
- ⚠️ **Node v24.13.1** (호스트 현재 설치 버전). `.nvmrc`는 사양대로 `22` 그대로 작성하되, 실제 실행은 v24로 수행. NestJS 10·pnpm 10 모두 Node 24 호환. **이 불일치는 P0.x 후속 task에서 nvm-windows 또는 fnm 도입으로 해결할 예정** — 이번엔 그대로 진행.

---

## 2. 이번 실행 (Phase B-2) 지시

1. **원지시 + 1차 보강 모든 §1~§4 결정사항** (build=tsc, NestJS skel 두 파일, path alias 미사용, dist gitignore, 요약본 D5 양식)을 **그대로** 적용.

2. **실행 순서**: 1차 보강 §2의 12단계 중 **단계 1 (`pnpm --version`)에서 더 이상 실패하지 않을 것** — 이미 pnpm 10.33.4가 PATH에 있음. `npm install -g pnpm`은 **실행 금지** (이미 설치됨).

3. **요약본** `docs/codex-log/2026-05-21-P0.1-monorepo.md`:
   - **기존 실패 보고 요약본을 덮어쓰지 말 것.** 대신 **append 또는 새 섹션 추가**로 "Phase B-1 실패 → Phase B-2 재실행 → 최종 결과"를 모두 보존.
   - 또는 파일명을 `docs/codex-log/2026-05-21-P0.1-monorepo-attempt2.md`로 새로 만들고, 기존 파일은 그대로 둘 것 (Phase B-1 기록 보존). 어느 쪽이든 1차 시도 흔적은 사라지지 않게.
   - **권장: 새 파일 `attempt2.md`로 작성** + 기존 파일은 그대로 둠.

4. **검증**: 1차 보강 §1 D5 양식 그대로 5개 게이트 결과 보고.
   - `pnpm install`
   - `pnpm -r build`
   - `npx tsc --noEmit` (각 패키지 또는 루트)
   - `npx vitest run`
   - Mountain 키워드 수동 스캔 (`rg`)

5. **최종 한 줄 메시지** (이전과 동일 양식):
   `[P0.1 Phase B-2 완료] 요약본: docs/codex-log/2026-05-21-P0.1-monorepo-attempt2.md | 게이트: <N/5 pass>`

---

## 3. 변동 없는 사항 (재확인용)

- Mountain 키워드 금지(원지시 §3 Security Wall).
- P1·P6·P7·F12 준수.
- 게이트 실패 시 정직 보고. "이 정도면" 식 마무리 금지(F14).
- Phase B 종료 후 어떤 git 명령(commit/push)도 실행 금지 — 그건 6단계 사장님 결재 사항.

---

*Supplement2 ID: P0.1-supplement2-2026-05-21 · pnpm 사전 설치·네트워크 허용 후 재실행*
