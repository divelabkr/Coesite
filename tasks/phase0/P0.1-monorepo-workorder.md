# [Phase 0 · Task 1] P0.1 Mono-repo 초기화 — Codex 작업지시

**발행자:** Claude (오케스트레이터)
**수신자:** Codex (gpt-5.5, approval_policy=never, sandbox=workspace-write)
**일자:** 2026-05-21
**작업 위치:** `C:\My_Project\Coesite\`

---

## 0. 이번 실행 모드 (중요 — 절대 준수)

**[Phase A — 이번 실행]** 구현 금지. **이해·계획만 출력하고 즉시 종료**한다.
- 어떤 파일도 생성·수정·삭제하지 않는다.
- `npm install` / `pnpm install` 도 실행 금지.
- `tsc` / `vitest` 도 실행 금지.
- 오직 아래 §6 "이해 확인 출력 양식"에 맞춰 텍스트 보고만 한다.

**[Phase B — 별도 호출]** Claude가 §6 출력을 검토 후, "구현 진행 승인"이라는 명시적 후속 작업지시를 받으면 그때 구현한다. 이번 호출에서는 절대 구현하지 않는다.

---

## 1. 목표

4개 패키지로 구성된 Mono-repo 초기화.

- `packages/types` — 공유 타입 (Coesite 전체)
- `packages/utils` — 공유 유틸 (hash, crypto, jwt)
- `packages/api` — NestJS 백엔드 (메인)
- `packages/sdk` — Phase 6에서 채움 (지금은 skel만)

---

## 2. 작업 범위 (확정)

1. **pnpm workspace 초기화** — 루트 `pnpm-workspace.yaml` 작성.
2. **각 패키지 package.json** — 이름 규약:
   - `@coesite/types`
   - `@coesite/utils`
   - `@coesite/api`
   - `@coesite/sdk`
3. **루트 tsconfig.base.json** + 각 패키지 `tsconfig.json` (extends base).
4. **루트 파일들** — `.gitignore`, `.editorconfig`, `.nvmrc` (값: `22`).
5. **루트 package.json scripts** — `dev`, `build`, `test`, `scan`, `lint`.
6. **각 패키지에 최소 진입 파일** — 빌드 통과를 위한 `src/index.ts` (export 빈 객체라도 좋음).
7. **skel 테스트 1건** — `packages/utils/test/sanity.test.ts` 같은 위치에 vitest "1 + 1 === 2" 한 줄. (Test Sentinel 요구.)

---

## 3. 제약 (절대 위반 금지)

### 기술 제약
- TypeScript **5.4** strict 모드 (`"strict": true`).
- Node.js **22** (`.nvmrc` = `22`).
- NestJS **10** — `packages/api`에만 (다른 패키지는 NestJS 의존성 금지).
- 패키지 import 별칭: `@coesite/types`, `@coesite/utils` (워크스페이스 protocol `workspace:*` 사용).
- 패키지 매니저: **pnpm**.

### Coesite 헌법 제약 (01-CLAUDE.md P1~P10)
- **P1 비판단**: `tensorflow` / `pytorch` / `onnx` / `*.predict(` / `new NeuralNet` import 금지. (이번 task에선 등장할 일 없음이지만 확인.)
- **P6 결합의존**: `packages/api`는 향후 `@coesite/types` + `@coesite/utils` 모두 의존해야 함 (지금은 skel이므로 의존 선언만 걸어둘 것).
- **P7 환각 금지**: 존재하지 않는 import·경로 참조 금지. 모든 path alias는 실제 폴더와 매칭.
- **F12 시크릿 하드코딩 금지**: `.env` / placeholder만, 실제 값 절대 코드 삽입 금지.

### Security Wall (04-SECURITY-WALL.md)
- **Mountain 키워드 절대 등장 금지** (변수명·주석·README·커밋 메시지 어디에도):
  04-SECURITY-WALL.md §1 Tier 1 목록 전체.
- 04-SECURITY-WALL.md §8 변형도 금지.
- 위반 탐지 시 즉시 작업 중단 + 보고.

---

## 4. 산출 디렉토리 구조 (Phase B 완료 후 모습)

```
Coesite/
├── pnpm-workspace.yaml
├── package.json                  # 루트 (scripts: dev/build/test/scan/lint)
├── tsconfig.base.json
├── .gitignore
├── .editorconfig
├── .nvmrc                        # 22
├── packages/
│   ├── types/
│   │   ├── package.json          # @coesite/types
│   │   ├── tsconfig.json
│   │   └── src/index.ts
│   ├── utils/
│   │   ├── package.json          # @coesite/utils
│   │   ├── tsconfig.json
│   │   ├── src/index.ts
│   │   └── test/sanity.test.ts   # vitest skel
│   ├── api/
│   │   ├── package.json          # @coesite/api (NestJS 10)
│   │   ├── tsconfig.json
│   │   └── src/main.ts           # NestFactory.create 진입점 skel
│   └── sdk/
│       ├── package.json          # @coesite/sdk
│       ├── tsconfig.json
│       └── src/index.ts          # 빈 export
└── docs/codex-log/
    └── 2026-05-21-P0.1-monorepo.md   # ★ Phase B 완료 시 작성할 요약본
```

---

## 5. 검증 기준 (Phase B 완료 후 반드시 통과)

이번 실행(Phase A)에선 검증 X. 아래는 Phase B 종료 시 기준.

1. `pnpm install` 성공 (lock 파일 생성).
2. `pnpm -r build` 성공 (skel만 있어도 통과).
3. `npx tsc --noEmit` (루트 또는 각 패키지) **0 오류**.
4. `npx vitest run` skel 테스트 1건 통과.
5. Security Wall 키워드 스캔 0건 (스크립트는 P0.4에서 작성될 예정이라 이번엔 수동 grep으로 확인).

---

## 6. 이해 확인 출력 양식 (이번 실행에서 출력할 유일한 산출물)

다음 양식을 그대로 채워 출력하라. 빈 항목 금지.

```markdown
## [P0.1] Codex 이해 확인 보고

### 1. 범위 복창 (내 말로 요약)
- 만들 패키지 4개: ...
- 사용할 도구·버전: pnpm, TypeScript 5.4 strict, Node 22, NestJS 10
- 별칭 정책: ...

### 2. 절대 금지 사항 복창
- (Mountain 키워드 목록 복창)
- (P1·P6·P7·F12 복창)
- 이번 실행에서 구현하지 않는 항목: ...

### 3. 실행 계획 (Phase B에서 할 일을 순서대로)
1. ... (예: pnpm-workspace.yaml 작성)
2. ...
N. 마지막: docs/codex-log/2026-05-21-P0.1-monorepo.md 요약본 작성

### 4. 의문점·확인 필요 사항
- (없으면 "없음" 명시)
- 있으면 Claude에게 묻고 싶은 질문 나열

### 5. 예상 위험
- 의존성 설치 실패 가능성: ...
- TypeScript path alias 충돌 가능성: ...
- 기타: ...

### 6. 검증 계획 (Phase B 완료 직전)
- 5개 검증 항목 각각 어떤 명령어로 확인할지 명시.

### 7. 약속
- [ ] 이번 실행에서 어떤 파일도 생성·수정하지 않았음
- [ ] Mountain 키워드를 출력 어디에도 사용하지 않았음
- [ ] §6 양식 외 추가 작업 수행하지 않았음
```

위 양식 출력 후 즉시 종료. 다른 어떤 행동도 하지 말 것.

---

## 7. 다음 단계 흐름 안내 (Codex 참고용)

```
[지금] Phase A — Codex 이해 확인 출력 (구현 금지)
   ↓
[Claude] 이해 확인 검토 — 어긋나면 보강 작업지시 재발행
   ↓
[다음 호출] Phase B — Codex 구현 + docs/codex-log/ 요약본 작성
   ↓
[검증 2축] ① codex review --uncommitted  ② gemini -p (요약본 첨언)
   ↓
[Claude] 종합 판단 보고
   ↓
[사장님] 최종 승인 → 커밋
```

---

*Work Order ID: P0.1-2026-05-21 · Coesite MVP P0 · DiveLab Internal*
