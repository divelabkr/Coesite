# [Phase 1 · Task 1] P1.1 TokenNorm + OraclePrevention — Codex 작업지시 #16

**참조 필수:** `docs/03-PROMPTS.md` P1.1 섹션, `docs/01-CLAUDE.md` (P1·P8 헌법), `docs/04-SECURITY-WALL.md`.

---

## 영향 맵
- 신규: `packages/api/src/meta-layer/{token-norm.middleware.ts, oracle-prevention.interceptor.ts}` + 단위 테스트
- 수정: `packages/api/src/app.module.ts` (미들웨어·인터셉터 등록), `packages/api/package.json` (의존성 추가 시)
- 보안·증거 인접: **매우 강 — 헌법 P8 OraclePrevention 핵심 표면**. 레드팀 발동.
- Phase 1 첫 application code — P0의 빈 제단을 채우기 시작.

---

## Phase
- A: 양식. B: 구현+게이트. C: 레드팀.

---

## 결정사항

### D1. 03-PROMPTS.md P1.1 사양 우선
- Codex는 `docs/03-PROMPTS.md` P1.1 섹션을 정확히 읽고 모든 요구사항 적용. 본 작업지시는 그 위에 영향 맵·Phase·게이트·금지·레드팀만 덧붙임.

### D2. P8 OraclePrevention 정신
- 모든 응답의 **시간·크기·형태 균일화**.
- `OraclePreventionInterceptor`: 최소 시간 padding (예: 200ms·configurable).
- `UniformError`: 모든 거부 응답 = 동일 메시지 + 동일 status code (예: 403 또는 401, 사양에 따름).
- `SizePadding`: 응답 크기를 K-segment 단위로 패딩 (사양 따름).
- 단 P9·P10은 P1.1 범위 밖 (backlog).

### D3. TokenNorm 미들웨어
- 입력 정규화 (대소문자·zero-width·공백·homoglyph 부분 처리).
- 정규화 후 원본·정규화본 모두 AuditLog에 기록할 수 있는 형태로(실제 INSERT는 Phase 1 후속 task).

### D4. NestJS 통합
- 미들웨어는 `app.module.ts`의 `configure(consumer)` 또는 `MiddlewareConsumer`로 등록.
- 인터셉터는 `APP_INTERCEPTOR` provider로 글로벌 등록.
- 의존성 추가 시 `packages/api/package.json`에 명시 + `pnpm install`.

### D5. 단위 테스트
- vitest로 각 미들웨어·인터셉터 단위 테스트 최소 5개 (정상·경계·거부·시간 padding·균일 응답).
- 회귀 PASS 필수.

### D6. P1 비판단 절대 준수
- AI/ML import 절대 금지 (TokenNorm·OraclePrevention 둘 다 규칙 기반).
- `.predict()` / dynamic require / bracket alias 등 우회 금지.

### D7. 자체 codex review 호출 금지 (sub-process 401 실증) — Claude 외부 호출.

---

## 게이트 (10개)

1. `pnpm install --frozen-lockfile` (또는 deps 추가 시 lock 갱신 후 frozen)
2. `pnpm prisma:generate` 회귀 PASS
3. `pnpm -r build` PASS (TypeScript 5.4 strict)
4. `pnpm -r exec tsc --noEmit` 0 오류
5. `pnpm test` 신규 5+ 케이스 포함 모두 PASS
6. `bash scripts/scan-principles.sh` P1~P10 0 위반 (특히 P1·P8 강화 검증)
7. `bash scripts/security-wall.sh` Mountain 0건
8. 부팅 sanity (`pnpm --filter @coesite/api start` 5초 헬스 확인)
9. CI YAML 정합 (변경 시)
10. 회귀: P0 산출물(prisma migration·docker compose) 무손상

Docker 부재 시 8은 NOT_RUN 정직.

---

## 절대 금지
영향 맵 밖 파일 수정 / Mountain 키워드 / 판단형 표현(eligible·decision·shouldPay) / silent error / git commit·push / "이 정도면" 식 마무리.

---

## 레드팀 관점 (Phase C — 8개)
1. **P1 비판단 우회** — AI 라이브러리 transitive deps·dynamic load·문자열 조립
2. **P8 시간 누설** — padding 단순 sleep이면 CPU 사용량으로 누설
3. **P8 크기 누설** — K-segment 단위 외의 길이 노출 (헤더·압축)
4. **P8 형태 누설** — error code·exception 패턴 차이
5. **TokenNorm bypass** — Unicode normalization 회피·bidi·composed/decomposed
6. **NestJS 인터셉터 순서** — 다른 인터셉터·필터·가드와 충돌
7. **Audit 누락** — 정규화 결과를 audit에 안 남기면 P5 prevHash 의도 약화
8. **테스트 안전감** — 5+ 케이스가 진짜 공격 시나리오 커버하는가

심각도: BLOCKER/MAJOR/MINOR/SUGGESTION.

---

## Phase A 양식

```markdown
## [P1.1] Codex 이해 확인 (Phase A)

1. 03-PROMPTS.md P1.1 사양 핵심 복창 (한 줄씩)
2. 영향 맵 대조 (✅·❌)
3. D1~D7 처리 방안
4. 10게이트 명령어
5. Docker 부재 시 처리
6. 의문점
7. 위험 3개
8. 레드팀 우선 시각
9. 약속 4개 [x]
```

---

*Work Order ID: P1.1-2026-05-22*
