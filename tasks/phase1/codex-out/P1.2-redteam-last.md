수정 없음; BLOCKER(A): 성공은 middleware 시간을 제외하고 거부는 포함해 timing oracle 가능(`token-norm.middleware.ts:32`, `oracle-prevention.interceptor.ts:21`), 성공 payload 크기별 `Content-Length`가 512/1024/...로 갈라져 size leak(`size-padding.util.ts:24`, 통계 e2e는 소형 `/ok`만 검증), rejection EML 값이 `source/statusCode`로 달라져 원인 oracle 가능(`oracle-prevention.service.ts:87`); BLOCKER(B): final이 “내부 상태 외부 노출 X”와 “EML 헤더 첨부”를 동시에 요구해 모순(`P1.2...final.md:52`, `:63`)하고 게이트가 값 동일성/전체 경로를 강제하지 않아 false PASS 가능; MAJOR(A): exception filter는 예외 발생 전 시간을 padding 기준에 포함하지 않아 lifecycle leak(`uniform-error.filter.ts:26`), aggregate body/node cap 부재로 fixed-point DoS 가능(`token-norm.service.ts:102`), 압축 비활성화는 앱 내부 grep 수준이라 proxy/후등록 middleware 우회 여지; MAJOR(B): 게이트 7은 `stddev<5ms`를 요구하지만 테스트는 mean 차이/p-value만 본다(`oracle-prevention.e2e.test.ts:124`), malformed URL은 service 단위 위주라 HTTP path/query 파서 경로 우회 검증이 약함; MINOR: 로그가 민감 원문 일부를 담을 수 있고(`token-norm.middleware.ts:62`) `headersSent`는 console 후 종료라 silent divergence 가능(`oracle-prevention.service.ts:72`); SUGGESTION: 게이트를 “경로별 동일 EML 값/동일 Content-Length/동일 header value/성공 대형 payload/지연 exception/middleware reject/malformed HTTP URL/proxy compression” 매트릭스로 고정해야 함.