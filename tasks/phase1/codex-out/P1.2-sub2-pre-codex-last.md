[sub-2 검토] **불통과 / 잔존 3건**

1. **E3 모순**: `SECRET_PEPPER`가 모듈 고정이면 같은 finding 100회 EML이 같아짐. “모두 다름” 게이트와 E6 “정상·거부 통계 구별 불가”에 불일치. 우회 가능.

2. **영향 맵 누락**: E1/E2/E4 구현에는 `types.ts`, `size-padding.util.ts`, `oracle-prevention.interceptor.ts` 수정이 사실상 필요함. 그런데 “영향 맵 밖 파일 수정 금지”와 충돌.

3. **E2 cap 사양 불충분**: “body 잘림” 방식이 JSON 무결성/외부 size bucket 누출/`payload-truncated` 메타 위치를 명확히 못 박지 않음. `>4096` 실제 절단 게이트도 필요.

나머지 E1/E4/E5/E6의 방향성은 헌법상 P2/P8 보강 취지에 부합. 단, 위 3건 보정 전에는 구현 지시로 바로 내려가면 안 됩니다.