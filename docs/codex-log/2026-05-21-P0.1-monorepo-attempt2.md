## [Phase 0 · Task 1] 작업 보고

### 변경 사항
- 추가 파일: `pnpm-workspace.yaml`, `package.json`, `pnpm-lock.yaml`, `tsconfig.base.json`, `.gitignore`, `.editorconfig`, `.nvmrc`, `packages/types/package.json`, `packages/types/tsconfig.json`, `packages/types/src/index.ts`, `packages/utils/package.json`, `packages/utils/tsconfig.json`, `packages/utils/src/index.ts`, `packages/utils/test/sanity.test.ts`, `packages/api/package.json`, `packages/api/tsconfig.json`, `packages/api/src/main.ts`, `packages/api/src/app.module.ts`, `packages/sdk/package.json`, `packages/sdk/tsconfig.json`, `packages/sdk/src/index.ts`, `docs/codex-log/2026-05-21-P0.1-monorepo-attempt2.md`
- 수정 파일: 없음
- 삭제 파일: 없음
- 참고: 기존 1차 실패 보고 `docs/codex-log/2026-05-21-P0.1-monorepo.md`는 보존함. `packages/*/dist/`는 빌드 산출물이며 `.gitignore` 대상임.

### 게이트 결과
- pnpm install: 성공. 최종 재실행 1.4초. 초기 설치는 15.3초 소요.
- pnpm -r build: 성공. 최종 재확인 2.96초.
- tsc --noEmit: 0 오류. `npx tsc --noEmit -p`를 4개 패키지에 각각 실행, 4.82초.
- vitest run: 통과. 1개 파일, 1개 테스트 통과. 최종 측정 1.97초.
- 수동 스캔 (금지어 rg): 0건. 생성·수정 산출물 범위에서 `rg --fixed-strings --line-number --no-heading --hidden` 실행.

### P1~P10 자가 진단
- P1 비판단: ✅ 판단형 로직·필드·표현을 코드에 추가하지 않음.
- P2 Fail-Closed: N/A (P0.1 범위 아님). 설치·빌드·검증 실패는 중단 후 원인 확인 방식으로 처리함.
- P3 WORM: N/A
- P4 HumanGate: N/A
- P5 AttestationChain: N/A
- P6 결합의존: ✅ `@coesite/api`가 `@coesite/types`, `@coesite/utils`를 `workspace:*`로 의존 선언함.
- P7 환각 금지: ✅ 실제 파일과 패키지명만 참조함. `tsconfig` path alias는 사용하지 않음.
- P8 Oracle Prevention: N/A
- P9 Trust Metabolism: N/A
- P10 Consensus: N/A

### Wall 검증
- 금지어 탐지: 0건.
- 스캔 범위: 루트 신규 설정 파일, `pnpm-lock.yaml`, `packages/`, 이 요약본.
- 비고: 작업지시 원문과 정책 문서에는 금지어 목록 자체가 있으므로, 이번 검증에서는 Phase B-2 생성·수정 산출물만 대상으로 삼음.

### 다음 액션
- P0.2 Docker Compose 환경을 별도 작업지시로 진행 제안.

### 리스크 / 미해결
- 현재 호스트 실행 Node는 v24 계열이고 `.nvmrc`는 사양대로 `22`임. 런타임 버전 정합은 후속 task에서 정리 필요.
- API는 NestJS 최소 부팅 골격만 있음. Controller, Service, DTO는 이번 범위 밖이라 추가하지 않음.
- `@coesite/sdk`는 Phase 6 전까지 빈 진입점만 제공함.
