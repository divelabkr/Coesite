# 2026-05-25 P1.2-sub2 workorder 보정

## 무엇을 했나
- `tasks/phase1/P1.2-sub2-essential-workorder.md`를 Revised v2로 보정했다.
- 기존 잔존 3건을 닫았다: E3 EML 반복값 모순, 영향 맵 누락, E2 size cap/JSON 무결성 부족.
- Codex-Reviewer와 Test Sentinel의 CONDITIONAL GO 조건을 반영했다.
- Gemini 검토는 인증 설정 부재로 NOT_RUN 처리했다.
- RedTeam 검토는 장시간 미완료라 종료했고, 구현 전 좁은 범위 재검토를 필수 게이트로 남겼다.

## 왜
- 사용자가 “확인되었으면 진행”을 승인했고, WBS상 가장 작은 다음 작업은 A-00~A-01(workorder 보정 + 영향 맵 5축 재작성)이었다.
- 기존 workorder는 구현에 바로 들어가면 deterministic EML, JSON 깨짐, 영향 맵 누락 위험이 있었다.

## 변경 파일
- `tasks/phase1/P1.2-sub2-essential-workorder.md` — 영향 맵 5축, file:line 호출부, dirty worktree 기준, safe summary, nonce provider, coverage/heavy gate, NO-GO 조건 보강.
- `docs/codex-log/2026-05-25-2255-p1.2-sub2-workorder-revised.md` — 이번 작업 요약.

## 검증 결과
- Codex-Reviewer — CONDITIONAL GO. P0 차단 없음. file:line, Mythos 게이트, timing/heavy 분리를 조건으로 제시했고 반영했다.
- Codex-Test Sentinel — CONDITIONAL GO. coverage gate, 순환 객체/BigInt/대형 payload, nonce mock, 성공 경로 header leak, e2e sandbox NO-GO 조건을 제시했고 반영했다.
- Codex-RedTeam — NOT_RUN/INCOMPLETE. 장시간 실행 중이라 종료. 구현 전 좁은 범위로 재실행 필요.
- Gemini — NOT_RUN. `GEMINI_API_KEY` 등 인증 설정이 없어 실행 불가.
- `bash scripts/security-wall.sh` — PASS, Mountain 0 / Mine 0 / Vein 0.
- `bash scripts/scan-principles.sh` — PASS, P1~P10 위반 0.
- 전체 `pnpm test`는 workorder 문서 변경만이라 실행하지 않았다.

## 리스크 / 미해결
- 아직 코드 구현은 시작하지 않았다.
- 구현 전 Phase A 이해 확인이 필요하다.
- RedTeam 재검토가 완료되지 않으면 구현 GO가 아니라 CONDITIONAL GO 상태다.
- coverage 80%는 문서 기준이지만 현재 package script에는 없으므로, 이번 구현에서 NOT_RUN을 명시하거나 별도 A-1 작업으로 먼저 추가해야 한다.
